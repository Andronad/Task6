import { MyColorOnDblClickDirective } from './my-color-on-dbl-click.directive';

describe('MyColorOnDblClickDirective', () => {
  it('should create an instance', () => {
    const directive = new MyColorOnDblClickDirective();
    expect(directive).toBeTruthy();
  });
});
